/**
 * Aether wire protocol.
 *
 * Placeholder — replaced by Buf-generated Protobuf types + the shared event
 * catalog in PR-06. For now it only proves the TS lint/typecheck/test lane.
 */
export const PROTOCOL_VERSION = 1 as const;
